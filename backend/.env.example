MONGODB_URI=
DATABASE_NAME=

# Data pipeline seed target — a SEPARATE test cluster, never the runtime one above.
# Used only by scripts/data_pipeline/load_to_mongo.py.
SEED_MONGODB_URI=
SEED_DATABASE_NAME=

ORIGINS=http://localhost:3000
